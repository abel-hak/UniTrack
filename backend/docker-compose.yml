services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: unitrack
      POSTGRES_PASSWORD: unitrack
      POSTGRES_DB: unitrack
    ports:
      - "5432:5432"
    volumes:
      - unitrack_pg:/var/lib/postgresql/data

volumes:
  unitrack_pg:

